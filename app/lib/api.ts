import React from 'react';

// This file previously contained helper stubs and lived inside the `app/` folder,
// which caused the router to treat it as a route and warn about a missing
// default export. Exporting a trivial component here quiets the router while
// keeping the file harmless.
export default function _LibPlaceholder() {
  return null;
}
